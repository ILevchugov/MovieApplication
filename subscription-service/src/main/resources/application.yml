server:
  port : 8090
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  flyway:
    baseline-on-migrate: true

logging:
  level:
    org:
      hibernate:
        type: trace